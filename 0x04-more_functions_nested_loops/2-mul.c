@@ -1,11 +1,13 @@
#include "main.h"

/**
 * mul - Entry point
 * @a: input
 * @b: input
 * Return: a * b
 */
 * mul - multiplies two integers
 * @a: multiplies with @b
 * @b: multiplies with @a
 * Return: multiplication of @a and @b
**/

int mul(int a, int b)
{
return (a * b);
	return (a * b);
}
